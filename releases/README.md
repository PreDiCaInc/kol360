# Release docs

Per-release handoff + soak-check documents for the prod team. Cut at every `prod-rel-X.Y.Z` tag.

## Convention

Each release has two files:

| File | For | When |
|---|---|---|
| `prod-rel-X.Y.Z-handoff.md` | Prod-team summary: what changed, migrations, rollback shape | Cut at tag time |
| `prod-rel-X.Y.Z-soak-checks.md` | Phased checklist (sanity / functional smoke / 24h watch) | Same PR as the handoff |

Tag `prod-rel-X.Y.Z` is anchored at the merge commit on `main` for the corresponding `vX.Y.Z` version.

Older releases (prod-rel-3.2 through prod-rel-4.1.1) lived in `docs/releases/` before this folder existed; they were migrated here on 2026-05-28.

## Releases (newest first)

| Tag | Version | Headline |
|---|---|---|
| [5.0.1](prod-rel-5.0.1-handoff.md) | v1.19.2 | **Post-soak hygiene bundle.** Closes the 5-ticket punch-list pteam raised on prod-rel-5.0 (2 P2 + 3 P3) + a one-shot Cognito Ops sweep. **No migrations**, no product runtime path changed except the Insights KOL Explorer influencer-type filter dropdown (see #1). (1) `INFLUENCER_TYPES` in `packages/shared/src/schemas/insights-report.ts` extended with 8 post-retag categories (DED Trace / Industry / Glaucoma / Retina / Retired / Canada / Deceased / FDA) — greens the failing `insights-report.test.ts` "labels every KOL with a valid influencer type" assertion + restores the missing filter options in the KOL Explorer dropdown (2,261 retagged HCPs were unfilterable). (2) Dropped hardcoded `|| 'E2eTest@2024Secure#1'` password fallback from `e2e/web/insights-demographics-pie.spec.ts` + `e2e/web/tour.spec.ts` — now hard-errors on missing `E2E_TEST_PASSWORD` env, matching the rest of the suite; removes the credential string the Bio-Exec strip safety-scan flagged. (3) `e2e/cleanup-test-data.ts` reordered so `CampaignHcp` deletes fire before `Hcp.deleteMany` (FK violation was silently swallowed); catch-branch rewritten to log `✗ Failed:` matching the campaign-cleanup style — the trailing `✅` now reflects true state instead of a false-positive "successfully". (4) `e2e/api/brand-grid-survey-submit.test.ts` `beforeAll` switched from `createTestCampaign()` (which passed the `cleanup:all`-deleted `SURVEY_TEMPLATE_ID`) to `createCampaign()` with no `surveyTemplateId` — test already Prisma-inserts its own SurveyQuestion so the template was unused. Unblocks targeted rerun after any level of cleanup and closes the Phase B2 respondent-side grid submit verification gap flagged in the 5.0 soak. (5) `e2e/package.json` gains `prisma:generate` helper + `pretest:*` pnpm pre-hooks for every shared-DB test entry point (`test:api:test:auth`, `test:api:aws:auth`, `test:workflow:*`, `test:all:aws:auth`) — schema-changing releases no longer surface `PrismaClientValidationError: Unknown argument …` from stale pnpm-cached clients. **Cognito Ops sweep (out-of-band, not code):** 251 stale `e2e-*` users deleted from `us-east-2_63CJVTAV9`, all pre-dating the v1.18.3 `afterAll` cleanup fix; original 2026-07-14 sweep used `--limit 60` and missed 191 tail rows, closed via paginated audit + per-username `admin-get-user` ground-truth. Zero users created post-fix — the cleanup fix itself is working; residue was historical accumulation. Ticket 6 option (c) (periodic Lambda) dropped since post-fix leakage is zero. Rollback shape: revert PR — no schema, no Cognito state to unwind. |
| [5.0](prod-rel-5.0-handoff.md) | v1.19.1 | **Brand-Affinity Grid feature complete (Phases 1 + 2 + 3) + MINC → OneKey ID rename + hardening pass.** Milestone tag — first customer-visible major feature since the 4.x line, plus CA identifier vocabulary rename with a DB migration. Two migrations run in order: (1) `20260710_add_brand_affinity_grid` adds new tables (`CampaignBrandOption`, `NominationBrandFlag`), the `BrandFlagType` enum, and two columns (`Campaign.brandsFrozenAt`, `SurveyQuestion.useBrandGrid`); (2) `20260716_rename_minc_to_onekey_id` flips existing `Hcp.nationalIdType = 'MINC'` rows to `'ONEKEY_ID'`. Both idempotent. **Brand-Affinity Grid**: admin configures per-campaign brand list + toggles grid per nomination question; respondent survey renders inline table (brand chips + Neutral + Unknown, mutually exclusive between brand set and sentinels, auto-swap on click); submit persists `NominationBrandFlag` rows in the same transaction as response completion; Sociometric Summary gains per-brand cluster columns after Biased + a two-state view toggle (All categories ↔ Bias focus); grid-mode `biasedLeaders` counts SUM of BRAND flags per HCP (per pteam decision — every check counts, not one per nomination); mixed-brand-list cross-campaign analyses hide the per-brand cluster and show only the Biased total. Classic (non-grid) campaigns are unaffected throughout. **OneKey ID rename**: MINC vocabulary swapped to IQVIA OneKey Reference terminology everywhere (display + enum + DB rows); format also relaxed from strict `CAMD########` to any 10-or-12 alphanumeric characters after normalization, unblocking real CA HCP data via the Canada HCP table import; CSV column headers `MINC`/`minc` and `OneKey ID`/`OneKey`/`OneKeyID`/`onekey`/`onekey_id` all accepted for backward compat. **Hardening**: `POST /campaigns` silent-drop of `showTopicsDiscussed` fixed (same anti-pattern as the `excludeInternalEmails` bug); `ChartTableToggle` wraps chart children in `w-full` so future chart types don't need the pie-blank inline workaround; Add Client modal now scrolls on short viewports; e2e cleanup FK-order fixed in both the script + the API's `forceDeleteTestCampaign` (was leaking >500 stale campaigns on test); e2e Cognito test-user cleanup fires in afterAll (was accumulating ~60 stale users on the shared pool). Verified: `v1.19.1` on test env, 303/303 e2e workflow pass. |
| [4.1.57](prod-rel-4.1.57-handoff.md) | v1.17.77 | **Insights Use Cases guide v1.1 image sweep + Case 5 tour anchor fix + drawer secondary-image render**. (a) Walked the Sun Pharma Case Study v1.1 docx systematically (r:embed → document-order → case/step context), swapped every `apps/web/public/help/insights-guide/case-*.png` to match v1.1 exactly. Net: 3 stale v1.0-era files removed (case-1-step-4, case-1-step-6, case-5-step-1 — v1.1 has no images at those step slots), 1 new secondary image added (case-1-step-5b — KOL Profile drill-down as Step 5's second capture), 1 image restored (case-3-step-2 — the July 1 finding proposed dropping it, but v1.1 as-shipped kept it; following the source of truth). 11 case-study images total after sweep, matching v1.1's 11 embedded images exactly. (b) Extended `GuideStep` interface with `image2` + `image2Alt` fields for steps that walk across two UI states (Case 1 Step 5's "click a KOL name → the profile drill-down"); insights-guide-content.tsx renders `image2` as a stacked secondary `<figure>` below the primary. (c) Case 5 tour anchor fix — Deep-dive steps 5 + 6 were targeting `leader-table` (which lives inside `<LeaderTable>` on the Benchmarking tab); Case 5 runs on the Total Weighted Score tab which renders `KolExplorerTab`. Result: `waitForElement` timed out silently, `tour.anchor_missing` telemetry fired, users got no highlight during "Sort by Trade Pubs" / "Read the composite". Fix: added `weighted-score-table` anchor to `TOUR_ANCHORS` + applied it to the KOL Explorer scores-table wrapper at kol-explorer.tsx:558 + retargeted Case 5's deep-dive steps. Cases 1-4 tours audited and verified aligned (no other mismatches found). No schema, no migration. |
| [4.1.56](prod-rel-4.1.56-handoff.md) | v1.17.76 | **Insights Demographics pie-chart-blank hotfix**. The Respondent Role pie chart on Insights → Demographics was rendering blank on first paint since v1.17.57 / prod-rel-4.1.35 (2026-06-22) when the ChartTableToggle wrapper was introduced. Recharts `ResponsiveContainer` inside `PieDistributionChart` measures the parent DOM element at mount, but the toggle wrapper's outer `<div>` didn't have a stable width at that moment; pie drew at 0×0 (invisible). Bar-chart cards in the same wrapper weren't affected. Fix (recommended #1 from the ticket): inline `style={{ width: '100%', minHeight: 288 }}` on the pie's parent `<div>` at two sites — the Respondent Role card (first card on the tab) and the Topics Discussed (Distribution) card. Bar chart cards unchanged. Added `e2e/web/insights-demographics-pie.spec.ts` — 2 tests asserting the pie's `<svg>` has non-zero dimensions on first paint + Chart↔Table toggle round-trip; skips gracefully on data-empty DAs. Follow-up (not in this release): fix #2 from the ticket — apply a `w-full` container to `ChartTableToggle`'s chart-branch so any future chart type dropped into it gets a stable parent by default. No schema, no migration. |
| [4.1.55](prod-rel-4.1.55-handoff.md) | v1.17.75 | **Interactive tour Phase 3 polish (FE-only)**. Three items on top of the tours system, DB-persisted completion deferred to a future release. (a) First-visit ring on the "How to…" button — 3-pulse teal ring runs for ~3.2s on new users, localStorage-gated (`kol360.how-to-cta-shown-at`), dismissed on click / dropdown open. Draws attention without a disruptive modal. `@media (prefers-reduced-motion: reduce)` fallback to a static outline. (b) "Show me the summary" popover per case study in the drawer — surfaces `caseStudy.tourSummary.bullets` (authored in 4.1.54) for users who want the digest without doing the walkthrough. Rendered via Radix Popover portal above the drawer. All 5 cases have bullets. (c) Playwright E2E specs — new `e2e/web/tour.spec.ts` (6 tests): dropdown lists all 5 cases, tour launches + shows step counter + target-click hint, full 7-step walk + checkpoint + Done fires all telemetry events (`tour.launched`, `.step_advanced`, `.checkpoint_reached`, `.completed`), completion checkmark flips, first-visit ring + localStorage suppression, summary popover reveals bullets. Runs via `pnpm test:web:test`. No schema, no migration. |
| [4.1.54](prod-rel-4.1.54-handoff.md) | v1.17.74 | **Tour Tier 1 muscle-memory + Cases 2-5 authored + case-1-step-3 image refresh + canada-hcp-isolation test fix**. (a) Steps whose `advanceOn: 'target-click'` (Open Benchmarking + Drill into a KOL in Case 1) no longer render a Next button — Skip + Prev only, with an inline hint "Click the highlighted element to continue" under the body. Empty-data fallback injects a Next button via `shepherdTour.getById().updateStepOptions()` if `waitForElement` times out so users aren't stranded. `tour.anchor_missing` telemetry still fires as the data-drift signal. (b) Cases 2-5 tour content authored — every case study in the "How to…" dropdown now launches a walkthrough (~25 authored steps across 4 cases + `tourSummary` bullets for the Phase 3 "Show me the summary" digest). Case 2: SECO Discussion + Advice Leaders (Benchmarking, GA/FL/AL + Optometrist). Case 3: SECO Rising Stars (Sociometric Leaders). Case 4: NY/NJ Symposium (Sociometric, NY/NJ + Ophthalmologist + Cornea/Dry Eye). Case 5: Trade Pub + National Leader composite (Total Weighted Score). Two new anchors applied: `sociometric-table` on the Sociometric tab Card + `filter-specialty` on the Core Focus MultiSelect. (c) `case-1-step-3.png` refreshed from the v1.1 docx (`image9.png` — fresher Bio-Exec-branded screenshot). (d) `e2e/api/canada-hcp-isolation.test.ts` leader-rankings call now passes `nominationType: 'NATIONAL_LEADER'` — v1.17.71's curation hardening made that a required query param and the test had been silently 400ing on every workflow run since. No schema, no migration. |
| [4.1.52](prod-rel-4.1.52-handoff.md) | v1.17.72 | **Interactive tour walkthroughs — Phase 1 pilot (Case Study 1)**. Ships the "Take the tour" interactive layer on top of the existing static Insights Use Cases guide. Shared primitives in `packages/shared/src/tours/` — TOUR_ANCHORS registry + `tourAnchor()` typed helper (compile-time drift-catcher), TourStep schema, `waitForElement` MutationObserver helper, `TourCompletionStore` + localStorage impl, `TourTelemetry` + logging stub (7 events defined). Runtime engine at `apps/web/src/components/tours/tour-provider.tsx` — dynamic-imports `shepherd.js` + `@floating-ui/dom` on first launch (~30KB doesn't ship on initial page load), URL state (`?tour=slug&step=N`), useReducedMotion, auto-switching tabs via real mousedown/mouseup/click (Radix TabsTrigger listens on mousedown), manual 900ms easeInOutCubic scroll via requestAnimationFrame. UX: "How to…" dropdown in Insights header (replaces old "Use Cases" direct-open button), modal overlay with Radix portal hoist so filter dropdowns aren't dimmed, step counter "STEP N OF 7" tag, checkpoint auto-inserted between Quick intro and Deep dive, completion checkmarks. Case Study 1 authored — 7 steps + auto-checkpoint. Deps: `shepherd.js@^15.2.2`, `react-shepherd@^7.0.4`, `@floating-ui/dom@^1.7.4`. No schema, no migration. |
| [4.1.51](prod-rel-4.1.51-handoff.md) | v1.17.71 | **Curation-svc integration hardening (Canada HCP)**. Closes the four sign-off conditions from the curation-svc team's review of the v1 integration spec. (a) `getBeIdRequestSchema.superRefine` now rejects unpaired `country`/`nationalIdType` combos (`'US' + 'MINC'`, `'CA' + 'NPI'`) with a 400 — previously a documented but unenforced invariant. (b) `POST /api/v1/hcps/get-beid` response now echoes persisted `country` + `nationalIdType` (additive; existing callers unaffected). (c) Spec v2 published: §5.3 "always send both" (was "optional but recommended"), new §7 Q5 pointing at merge-tombstone flow for MINC corrections, "Sun Pharma" placeholder swapped to "a US-region client" throughout, cross-licensing plan (Path B: mint under primary + capture alternate in `discoveredFrom.notes`), backfill identification method (curation-svc supplies beId list), rate-limit posture, state-field validation intent. Test coverage: 8 new unit tests on the schema pairing, 3 new e2e tests (CA happy path + both pairing rejections). No schema, no migration. |
| [4.1.50](prod-rel-4.1.50-handoff.md) | v1.17.70 | **Canada HCP support — Phase 2 polish + integration spec**. Closes the 3 residuals surfaced during the 4.1.49 post-merge review. (a) FE: Lite HCP scores table + Campaign Payments table now use `inferHcpIdLabel(items)` (backend selects extended to include `hcp.nationalIdType`). (b) BE: nomination `matchToNewHcp` now infers `country`/`nationalIdType` from identifier shape (`/^CAMD\d{8}$/i` → CA/MINC). Dormant today because `createNominatedHcpSchema` doesn't accept MINC input yet; closes the landmine before CA nominations flip on. (c) E2E: new `canada-hcp-isolation.test.ts` seeds a CA fixture via Prisma, verifies country filter + Insights isolation (KOL Explorer / Leader Rankings / Sociometric / KOL Profile drill-down) + cleanup — replaces the manual Phase B checklist steps from 4.1.49 soak. (d) `releases/curation-svc-canada-integration-spec-v1.md` — full integration spec for the kolcuration team to start sending `country` + `nationalIdType` in `get-beid` calls (zero-coordination rollout; kol360 already accepts both shapes). No schema, no migration. |
| [4.1.49](prod-rel-4.1.49-handoff.md) | v1.17.69 | **Canada HCP support — Phase 2**. Removes the gate that "no customer may create a CA client until Phase 2 ships" from 4.1.48. (a) Insights + KolAnalysis + Nomination-matching HCP queries all threaded with `WHERE country = client.defaultCountry`. `getKolProfile` + `explainHcp` reject cross-country deep-links as HCP-not-found. Nomination auto-match candidates across all 4 tiers (exact/last-name-partial/trigram/broad-partial) filter by `nomination.response.campaign.client.defaultCountry`. (b) Curation `get-beid` accepts optional `country` + `nationalIdType` (defaults 'US'/'NPI' so existing curation-svc clients keep working); persisted on created rows. (c) FE sweep — new `inferHcpIdLabel(items)` helper in `packages/shared`; HCP list column + CSV export, KOL Explorer CSV + Nominators SortableHeader, Sociometric CSV, Leader Table CSV all use it. Falls back to 'NPI' on empty data. No schema, no migration. |
| [4.1.48](prod-rel-4.1.48-handoff.md) | v1.17.68 | **Canada HCP support — Phase 1 (P1 for CA delivery).** Ticket [`docs/findings/canada-hcp-support-lite-plan-2026-06-25.md`](../docs/findings/canada-hcp-support-lite-plan-2026-06-25.md). Schema: `Hcp.country + Hcp.nationalIdType + Hcp.alternateIds` added (defaults 'US'/'NPI'); `Client.defaultCountry` added (default 'US'). Validation: MINC regex added (`CAMD########` after normalization stripping hyphens + uppercasing); customer-confirmed format. Bulk import route accepts `?country=US\|CA` query param picking the validator regime. HcpAlias MERGE branch now country-scoped so cross-country name collisions can't silently merge. FE: Client form gets HCP Country selector; HCP Import Dialog gets Country toggle with per-mode column-header copy; identifier display sites on `/admin/hcps` list + detail use new `formatHcpId()` / `getHcpIdValue()` helpers from `packages/shared`. Migration `20260703_add_country_and_nationalidtype_to_hcp` (idempotent). **Phase 2 deferred (documented in handoff)**: ~40-query Insights `WHERE country` threading, curation get-beid extension, CSV export column-header country awareness, remaining ~30 FE `.npi` label sites. Safe today because prod has zero CA HCPs. |
| [4.1.47](prod-rel-4.1.47-handoff.md) | v1.17.67 | **EmailDeliveryEvent scope gap fix (P1)** — closes the gap that made user-invite / invite-resend SES sends invisible in the DB and forced pteam to hand-grep CloudWatch on every "didn't get the invite" report (Michael Garanzini investigation, 2026-07-02). Schema: `EmailDeliveryEvent.campaignId + hcpId` relaxed to nullable, new `userId` column + index. Code: `sendUserInvitation` now writes an EDE row at send-time (same pattern as campaign paths since v1.17.37); both callers pass `userId + messageType` (`user_invite` vs `user_invite_resent`). SES → SNS → webhook correlates by `sesMessageId` as before — no webhook code change. **New endpoint** `GET /users/:id/latest-invite-event` returns the newest invite EDE row per user. **New users-page column "Last Invite"** — small `✓ Delivered / ⚠ Bounced / · Sent` chip per PENDING user, drives admin self-service. Migration: `20260702_relax_emaildeliveryevent_scope` (idempotent). Rollback path: redeploy prior code, leave schema widened. |
| [4.1.46](prod-rel-4.1.46-handoff.md) | v1.17.66 | Insights guide v1.1 image refresh from pteam ticket. (1) New consolidated **full KOL Profile drill-down screenshot** (Bio-Exec branded, shows Respondent Filters bar + Use Cases button + all demographic sub-charts) placed at Case Study 1 Step 5 slot. Replaces the prior score-table LIST view, which mismatched the Step 5 body copy ("Click any KOL's name to open the KOL Profile drill-down"). (2) Case Study 3 Step 2 image removed — the sort-arrow UI is self-evident; deleted screenshot didn't teach anything the user couldn't see in real time. Body text unchanged. Deviation from ticket noted in handoff: ticket recommended the new image at CS1 Step 3 slot, but source `.txt` + shipped `guide-content.ts` both have Step 3 as the "Respondent Role filter" step; Step 5 is where the Profile is first introduced. No schema, no migration. |
| [4.1.45](prod-rel-4.1.45-handoff.md) | v1.17.65 | Insights Use Cases polish — three UX fixes from pteam soak. (1) Intro-tab "Insights — Use Cases" link now opens the same right-side drawer the header "Use Cases" button does (was navigating to `/admin/dashboards/guide` as a full page). Less context loss for users reading Methodology + clicking through. (2) Drawer width bumped from 720px cap to 65vw capped at 1100px (≈ 60–70% of viewport on common laptop / monitor sizes). Step text + screenshots breathe better side-by-side. (3) Explicit favicon in root metadata so `/admin/dashboards/guide` (and other App Router branches without a co-located favicon) renders the KOL360 tab icon. (4) Tooling: `scripts/sync-hcps-from-prod.ts` now advances `beid_seq` past max synced beId at end of run — Postgres sequences don't auto-advance when rows are inserted with explicit values, so without this every API-driven HCP create after a sync would hit a unique-constraint collision on beId. Idempotent. Test DB hand-fixed during diagnosis. No schema, no migration. |
| [4.1.44](prod-rel-4.1.44-handoff.md) | v1.17.64 | One-paragraph follow-up to 4.1.43. Insights Introduction tab now ends with a sentence linking to the new Use Cases guide at `/admin/dashboards/guide`. Closes the discovery loop for users who land on Introduction by default — they get pointed at the guide without having to spot the "Use Cases" button in the header. Pure FE-only addition. No schema, no migration. |
| [4.1.43](prod-rel-4.1.43-handoff.md) | v1.17.63 | **Insights Use Cases guide in-product (P2 customer enablement).** Client admins can read 5 worked case studies + 4 practice scenarios without leaving the dashboard. Surfaces: (a) a "Use Cases" button in the Insights dashboard header opens a right-side drawer with the full guide; (b) the same content is bookmarkable at `/admin/dashboards/guide`; (c) per-tab `?` info popovers give a one-line orientation + deep-link into the relevant case studies; (d) first-visit auto-open (dismissible, localStorage-persisted). Brand-neutral — "Sun Pharma" labels from the source doc dropped in-product. New Sheet (side-drawer) primitive added under `components/ui/`. No new markdown library — content lives as a typed TypeScript module + screenshots under `apps/web/public/help/insights-guide/`. Existing Introduction tab kept in place; follow-up call to decide whether to fold/drop. Also folded in: 4 tooling commits already on dev (prod→test sync scripts with runtime IP pin). No schema, no migration. |
| [4.1.42](prod-rel-4.1.42-handoff.md) | v1.17.62 | Three pteam tickets, two P1. (1) **Email logo renders in browser-based webmail** (Outlook web / Gmail web / Yahoo Mail). Root cause was Next.js setting `Cross-Origin-Resource-Policy: same-origin` on every static path; webmail's image proxy fetched the asset but the browser blocked the render. Two coordinated fixes: inline the logo as a base64 data URI in email templates (`apps/api/src/assets/logo-data-uri.ts` generated from `apps/web/public/images/logo-white.png`) so the remote fetch is eliminated; loosen CORP to `cross-origin` on `/images/(.*)` and `/_next/static/(.*)` via a second header block in `next.config.mjs`. (2) **KOL Profile drill-down no longer leaks internal Bio-Exec nominators**: `getKolProfile` was treating `excludeInternalEmails` as an opt-in query param the FE never sent → Sun Pharma + B+L tenant-level "Exclude internal" toggles were silently ignored. Now derives the flag from `campaigns.some((c) => c.excludeInternalEmails)` (canonical pattern used by getSummary / getDemographics / getKolNominationMetadata). Also dropped dead `_excludeInternalEmails` from getLeaderRankings. Behavior-change note: Sun Pharma + B+L KOL Profile nominator counts drop by the number of internal @bio-exec.com nominations (e.g. Karpecki: −4). (3) **"Nominators" relabeled to "Nominations"** on the KOL Profile drill-down table — each row is one nomination event, a single nominator can produce multiple rows when nominating for multiple types. User-visible labels only. No schema, no migration. |
| [4.1.41](prod-rel-4.1.41-handoff.md) | v1.17.61 | Two pteam soak follow-ups from 4.1.40. (1) **`logo-white.png` shrunk 899 KB → 4.9 KB** (99.4 %). v1.17.60's render fix made the image LOOK right; this fixes the byte size that was still slow on mobile + getting dropped by Outlook proxies. Resampled to 93×72 (2× retina) via sips. (2) **77 lines of dead CLIENT_ADMIN branches removed** from `routes/users.ts` across invite / update / disable / enable / resend-invite / delete. Each route is already gated by `requirePlatformAdmin()` preHandler, so the inner role checks never ran. Pure refactor, behavior identical. No schema, no migration. |
| [4.1.40](prod-rel-4.1.40-handoff.md) | v1.17.60 | Three pteam tickets bundled. (1) **Outlook gradient + logo fallback (P1)**: every customer-facing email (welcome / survey-invite / survey-reminder) rendered with an invisible tagline + invisible CTA + broken-image logo in Outlook since v1.17.48 (Outlook drops CSS `linear-gradient` silently). Added `background-color: #147a6d` solid fallback before every gradient (6 sites), HTML `width="144" height="36"` attrs + `object-fit: contain` + `border: 0` on the 3 logo `<img>` tags. Non-Outlook clients unchanged. (2) **Client logo upload + 20KB cap + full settings inline (P2)**: client form dialog now has Upload \| Paste URL tabs; Upload reads via FileReader, enforces 20 KB binary cap, stores as `data:image/*` URI directly in `client.logoUrl` (no S3 infra needed). Zod schema extended to accept data URIs (32 KB char cap). Client detail page surfaces Logo (with preview) + Allowed Email Domains (Badge chips + destructive empty-state warning — matching the actual v1.17.19 behavior where empty = only @bio-exec.com staff invitable). (3) **Users page resend invite + delete (P2 follow-up)**: two new endpoints — `POST /users/:id/resend-invite` (rotates Cognito temp password via `AdminSetUserPasswordCommand`, re-sends branded invite; only PENDING) and `DELETE /users/:id` (hard delete Cognito + DB; self-delete blocked via `cognitoSub` match). Dropdown gets Resend Invite (PENDING) + Delete (always, two-step confirm). Also includes the held-back PARTIAL_UPDATE_HCP e2e fixture fix from the 4.1.39 cycle. No schema, no migration. |
| [4.1.39](prod-rel-4.1.39-handoff.md) | v1.17.59 | **HCP admin list export uncaps the 5k limit.** v1.17.58's "export full filtered list" fix used a single `limit=5000` re-fetch — customers with > 5k HCPs hit the hard cap and got silent truncation. Now paginates through all pages (1000 per page): page 1 fetched to learn total, remaining pages fired in parallel, concatenated. Falls back to the visible page on any fetch failure. FE-only. No schema, no migration. |
| [4.1.38](prod-rel-4.1.38-handoff.md) | v1.17.58 | Five-commit consolidated drop. Supersedes a held-back 4.1.37 (v1.17.57) draft. (1) **HCP importer UPDATE branch stale-snapshot race fix** (production code): v1.17.56's `row.X \|\| existing.X` fallback wrote back stale snapshot values for omitted columns under concurrent admin uploads → clobbered any concurrent edit. Now only sets columns the row provides; omits the rest from the Prisma `update` call. (2) **HCP admin list export covers the full filtered list** (production code, user-reported): `/admin/hcps` Export was building the CSV from the visible page (50 rows) instead of the active filter set. Re-fetches with `limit=5000` before building rows — matches the v1.17.32 pattern shipped on Sociometric Summary, KOL Explorer, Leader Rankings, Survey Status. Audit confirmed all 6 other export surfaces already did this; only this one was missed. (3) **Benchmarking (i) right-aligned in the colored title bar** for consistency with Demographics card placement (was next to the title text in v1.17.55). (4) **Chart/Table toggle on Demographics chart cards**: new affordance — right-aligned "Chart \| Table" toggle on each card; flips to a sortable, copy-friendly tabular view. Shared `ChartTableToggle` component; applied to 10 single-series cards. StackedBar-backed cards (Educational Resources, Social Media Rankings) skipped — multi-dimensional data needs a different table shape. (5-7) Three E2E test fixups (`e2e-maintenance-3-fixups`): influencer-type set updated to the v1.17.44 5-value list, ZodError-specific assertion loosened to status-only, match-count parity test pinned to new `STABLE_FIXTURE.PARITY_*` seed. No schema, no migration. |
| [4.1.36](prod-rel-4.1.36-handoff.md) | v1.17.56 | Three bundled items, each too small for its own cycle. (1) **KOL Profile drill-down — Apply Filters pattern**: the last remaining tab from the Track B Apply Filters work (4 converted in 4.1.33). Respondent-filter bar above the demographic sub-charts; Apply button + live "N nominators match" via `useNominatorMatchCount` (already shipped in 4.1.32). Backend `getKolProfile` accepts optional respondentFilters and intersects nominations with `getFilteredResponseIds`. (2) **Seg-only HCPs in analysis recalc**: pteam ask — "include all hcps who have the seg scores, even if they don't have nominations." `computePooled` now persists `HcpAnalysisScore` rows for every HCP with current `HcpDiseaseAreaScore` in the DA. Seg-only HCPs land with nominationCount=0, scoreSurvey=null, composite = segment-driven weighted sum (survey contributes 0). **CRITICAL**: requires Recalculate click on each prod analysis post-deploy to backfill. (3) **HCP importer UPDATE branch accepts partials**: P3 UX polish ticket. Validator restructured to per-branch — UPDATE accepts partial rows (NPI + any subset); CREATE + MERGE stay strict. Enables self-serve `NPI,City,State` / `NPI,Email` / `NPI,Specialty` uploads, removing direct-SQL ops sessions. 231/231 unit tests pass + 5 new e2e cases. No schema, no migration. |
| [4.1.35](prod-rel-4.1.35-handoff.md) | v1.17.55 | Three pteam-flagged Insights polish items surfaced during 4.1.34 inspection. (1) **Survey-source privacy strip**: `(i)` popover's "Source: <Campaign>" line now hidden for non-PLATFORM_ADMIN viewers (lite clients pool data across other clients' campaigns; surfacing source would leak cross-tenant origin). Route-level strip; service stays role-agnostic. (2) **Benchmarking `(i)` visibility fix**: moved the survey-question icon from a muted gray row above each panel's search into the colored title bar where the bold white panel name lives — the user's eye lands there. `LeaderTable` gains a `titleSuffix` slot; icon hardcoded `h-4 w-4` (was templated, fragile under Tailwind JIT). (3) **Realtime search**: search input on Sociometric Summary + KOL Explorer (Total Weighted Score) is no longer gated on Apply — fires 250ms after the last keystroke. Specialty / state / score / respondent filters still gate on Apply; Reset still clears search too. New `useDebouncedValue` hook in `use-filters.ts`. No schema, no migration. |
| [4.1.34](prod-rel-4.1.34-handoff.md) | v1.17.54 | Three Insights polish items bundled (each too small for its own release; they all landed on dev right after the 4.1.33 PR merged). (1) **Sociometric Leaders tab — duplicate block removed**: the "Per-Category Leader Tables" section below the matrix was feature-equivalent to the Benchmarking tab (same `useLeaderRankings` hook, same component shape, no filters); Benchmarking stays as the canonical surface. (2) **Influencer Type filter dropdown drift fix**: was hardcoded to `['National Leaders', 'Rising Stars', 'Regional Influencers']` and missed the v1.17.44 expansion ('Regional Leaders' + 'Pre-Emergent'); customers picking 'Regional Influencers' on prod got 0 results because no HCP was classified that way. Now DB-driven via `SELECT DISTINCT "influencerType" FROM "HcpDiseaseArea"`, same pattern as specialty/state/coreFocus. On prod Dry Eye the dropdown now correctly shows 4 values (Pre-Emergent: 2,261 HCPs, Regional Leaders: 1,291, Rising Stars: 319, National Leaders: 87). (3) **Survey-question (i) info popovers** on Benchmarking (per leader panel) and Demographics (per chart card) — two new additive endpoints (`/insights/:da/nomination-questions` + `/insights/:da/demographic-questions`); click the (i) → popover with the actual question text + source campaign. Most-recent-campaign tie-break when an analysis pools campaigns with slightly different question text. No schema, no migration. |
| [4.1.33](prod-rel-4.1.33-handoff.md) | v1.17.53 | **Track B frontend**: Apply Filters button + live "N match" indicator across 4 Insights tabs (Sociometric Summary, KOL Explorer, Demographics, Leader Rankings). Replaces the 4.1.32-and-before auto-fire pattern that cascaded 4–5 heavy aggregation queries during the customer demo. New shared infrastructure: `useFilters` hook (pending/applied + dirty/apply/reset), `useKolMatchCount` / `useRespondentMatchCount` hooks (debounced 250ms, fires only on dirty pending state), `<ApplyFilterControls>` component (Apply + Reset + live count + dirty styling). Pure UI change — uses match-count endpoints already shipped in 4.1.32. No schema, no migration. KOL Profile drill-down filter UI deferred to next PR (it has no existing filter bar to convert — new UI surface). |
| [4.1.32](prod-rel-4.1.32-handoff.md) | v1.17.52 | **Track B backend**: two additive `match-count` endpoints powering the live "N match" indicator next to the upcoming Apply Filters button. `/insights/:da/match-count?type=kols\|respondents` returns `{ count }` for the same filter set the corresponding full-aggregation endpoint will return as `total` (parity-guaranteed by E2E). `/insights/:da/kol-profile/:hcpId/match-count` returns distinct-nominator count. Reuses existing service helpers (resolveAnalysis, getFilteredResponseIds, computeRespondentFilteredCounts, resolveAccessibleCampaignIds) so semantics align by construction. **No UI changes ship in this release** — frontend Apply Filters rollout lands in a follow-on PR. No schema, no migration, low risk. |
| [4.1.31](prod-rel-4.1.31-handoff.md) | v1.17.51 | **perf-pass-C**: Insights endpoints under active respondent filters were 2-9s on prod (pteam ticket 2026-06-16). Two paired fixes. **#1** — `loadAnswersForRespondentFilter → computeFilteredResponseIds` collapsed into one `$queryRaw` `getFilteredResponseIds` (each active filter becomes one EXISTS clause, inactive filters splice out at zero cost). Shared across getLeaderRankings + getSociometricSummary + getDemographics. Eliminates the multi-MB answer payload + 7-pass JS aggregation. **#3** — idempotent composite index `SurveyResponse_campaignId_status_idx` on `(campaignId, status)` replaces the seq scan on every analysis-scoped path that gates by campaign + status. Original **#2** (KolExplorer / SocoSummary in-memory sort+page push to SQL) was dropped after EXPLAIN profiling showed the analysis HCP set is bounded (<1500) — marginal gain not worth the SQL complexity. One migration. Filter result counts must match v1.17.50 row-for-row — soak step B is parity-critical. |
| [4.1.30](prod-rel-4.1.30-handoff.md) | v1.17.50 | **P1 hotfix on 4.1.29**: completes the lite-client journey end-to-end. 4.1.29 fixed the routing + DA-list visibility but the per-endpoint `verifyDiseaseAreaAccess` gate + 3 service methods (`getSummary`, `getDemographics`, `getKolNominationMetadata`) still scoped by `clientId → OWNED campaigns` — lite clients own 0 → 403 ("error loading data") + empty payloads ("no data available"). Same OR / UNION pattern as 4.1.29: access gate accepts EITHER campaign or KolAnalysis ownership; campaign-aggregating service methods now use a new `resolveAccessibleCampaignIds` helper = (owned ∪ analysis-included). Regular clients with no cross-tenant analysis inclusions: zero behavior change (owned = analysis = same set). Pteam reported the bug immediately after 4.1.29 deploy testing on prod. No schema, no migration. |
| [4.1.29](prod-rel-4.1.29-handoff.md) | v1.17.49 | Two paired P1 fixes for the lite-client TEAM_MEMBER / CLIENT_ADMIN journey. **Bug #1 (FE)**: `/admin` now redirects non-PLATFORM_ADMIN users to `/admin/dashboards` (was: landed on the platform-admin dashboard page with no nav link to escape it). **Bug #2 (BE)**: `GET /insights/disease-areas` filter broadened to `OR { campaigns | kolAnalyses }` — lite clients have 0 campaigns by design and reach DAs only via `KolAnalysis`; the old AND-only filter silently hid them, so lite-client users saw "no disease areas available". No schema, no migration, structural e2e check added. |
| [4.1.28](prod-rel-4.1.28-handoff.md) | v1.17.48 | User invite email facelift — replaces Cognito's default one-line "username + temp password" plaintext with a branded SES template that mirrors the survey-invitation polish: gradient header w/ KOL360 logo, code-style credentials block, "Sign In to KOL360" CTA → `${APP_URL}/login`, role label, security note, plain-text fallback. App now generates the temp password locally and suppresses Cognito's default email. |
| [4.1.27](prod-rel-4.1.27-handoff.md) | v1.17.47 | 6 small UX wins: View Scores now scopes HCP list to the active disease area (Karpecki Medical-Oncology bug fix); Insights decile bar charts sort 1→10 (was: count desc); client form "Allowed Email Domains" input now accepts commas and spaces (was: eat-the-separator on every keystroke); Nominators table on KOL Profile gains Export Excel button + name-hyperlinks-when-scored; NPI moved inline next to the hero name on KOL Profile (was: stacked below). Additive API: nominator items gain `hasScores: boolean`; `byDecile` arrays now sorted ordinally on both demographics + kol-profile responses. |
| [4.1.26](prod-rel-4.1.26-handoff.md) | v1.17.46 | Single follow-on to 4.1.25: HCP NPI now renders directly under the name on the KOL Profile (the HCP detail view inside KOL Explorer). Pteam asked during the 4.1.25 review; landed after PR #172 merged. Tiles row stays separate — design decision logged. |
| [4.1.25](prod-rel-4.1.25-handoff.md) | v1.17.45 | Focused UX bundle: column-selector inline with Export on Insights tabs; CLIENT_ADMIN sidebar restricted to KOL Insights + default-collapsed; user profile moved from header top-right to sidebar bottom (matches Linear/Notion/Slack pattern) — header stripped to Breadcrumb + ClientBadge; View Scores page gated to PLATFORM_ADMIN + facelift (column selector, sticky NPI+Name, sortable Name/NPI/State/Specialty); 4 HCP-admin import buttons unified under the Upload icon + new "Import Segment Scores" surface; Nominators table on KOL Profile gains NPI column and gates Campaign column to PLATFORM_ADMIN. Small additive API: `/hcps` `sortBy`/`sortOrder`, nominator response gains `npi`. |
| [4.1.24](prod-rel-4.1.24-handoff.md) | v1.17.44 | Adds 2 influencer-type buckets ('Regional Leaders', 'Pre-Emergent') to the canonical list — same single-source-of-truth pattern as score-methodology. Dialog now shows the allowed types as a badge row + template auto-derives from the const so adding a future type is a single `INFLUENCER_TYPES` edit. Backend also accepts singular / hyphen-variant aliases (`Regional Leader`, `Pre Emergent`, `Preemergent`). No migration, no schema change. |
| [4.1.23](prod-rel-4.1.23-handoff.md) | v1.17.43 | **P1 hotfix on 4.1.22**: InfluencerTypeImportDialog rolled its own `authToken()` reading from localStorage keys the app doesn't use → every upload 401'd "Missing or invalid authorization header". Fixed by mirroring the `useImportHcps` mutation pattern (uses live Cognito `getToken()`). Plus 2 UX polish: dialog now accepts xlsx/xls/csv + has a Download Template button (consistent with HcpImportDialog), and Insights tables tightened (`min-w-[1600px]` dropped on KOL Explorer + `px-2` on score/category cells) so hiding columns via the selector actually frees horizontal space. |
| [4.1.22](prod-rel-4.1.22-handoff.md) | v1.17.42 | Data-team-managed `influencerType` classification per (HCP, disease area) — replaces algorithmic `determineInfluencerType()` from `compositeScore + scoreSurvey` thresholds. New "Import Influencer Types" button in HCP admin: select disease area, upload CSV (NPI,InfluencerType), 2-step preview ("Based on this file, 500 HCPs will be classified for Dry Eye"), confirm. No fallback when unset → coordinate deploy with data team. Plus bundled UI polish: sticky `# + Name` cols on Sociometric, column-visibility selector on KOL Explorer + Sociometric (default hides Degree + City), collapsed-sidebar click fix, PLATFORM_ADMIN view-as branding in Insights. |
| [4.1.21](prod-rel-4.1.21-handoff.md) | v1.17.41 | Single-line clarity fix on the 4.1.20 Survey Score tooltip — dropped Regional Leader from the "Not counted" list since no customer survey uses that enum value today (it's a reserved Prisma enum from v1.17.34 with zero in-use questions). Backend formula behavior unchanged. |
| [4.1.20](prod-rel-4.1.20-handoff.md) | v1.17.40 | `scoreSurvey` formula rewrite to match Sun Pharma's published "Total Sociometric Weighted Score" methodology — sum of nominations across 4 counted types (National, Discussion, Advice, Rising Star) ÷ max-such-sum × 100. Verified to the 2nd decimal across 2,301 HCPs in the customer's reference file. Plus in-product methodology tooltips on Survey + Composite + per-category column headers (single source of truth in `packages/shared/src/score-methodology.ts`) and sticky Name column on the KOL Explorer "Weighted Score" tab. **No auto-backfill** — existing Recalculate button is the rollout trigger. |
| [4.1.19](prod-rel-4.1.19-handoff.md) | v1.17.39 | Hotfix roll-forward on top of 4.1.18 — `POST /hcps/import` was 400ing on every upload because the new per-row audit `createMany` passed the Cognito sub as `userId` (FK→User.id). Caught by the v1.17.35 e2e tests during post-deploy soak. Pteam syncs **4.1.19** (not 4.1.18). |
| [4.1.18](prod-rel-4.1.18-handoff.md) | v1.17.35 → v1.17.38 | Four 2026-06-13 incident tickets in one release: HCP row-level audit foundation (`HcpImportBatch` + `Hcp.importBatchId` + per-row + dedicated email/specialty actions) + bulk-send `isPlaceholderEmail()` gate (closes the 269 invitations-into-the-void window) + SES delivery-event tracking (`EmailDeliveryEvent` + SNS handler + bounce-aware reminder gate) + survey-email mismatch surface on payment-export (no auto-update, no backfill). Plus a small payments-page HCP search carry-over. |
| [4.1.17](prod-rel-4.1.17-handoff.md) | v1.17.34 | HCP admin-page polish + nomination rematch: full-name search fix ("Paul Karpecki" returned 0 rows), NPI editable for PLATFORM_ADMIN with audit trail + 409 on collision, email-placeholder converted to a clickable chip, and a new "Change match" affordance on matched nominations (PLATFORM_ADMIN) that re-points the match via POST `/nominations/:nid/rematch` with full audit trail (`nomination.rematched`) |
| [4.1.16](prod-rel-4.1.16-handoff.md) | v1.17.33 | P1 customer fix: Sociometric Summary KOL-side filters (KOL State / Specialty / Influencer Type) were silently ignored — destructure read only the singular shape. Single-method fix + new e2e structural-check matrix that catches this bug class going forward |
| [4.1.15](prod-rel-4.1.15-handoff.md) | v1.17.32 | Insights polish: Sociometric Summary matrix column reorder + Biased column added + every Insights "Export to Excel" button now exports the FULL filtered list (was: current page only) with an NPI column |
| [4.1.14](prod-rel-4.1.14-handoff.md) | v1.17.31 | P1 hotfix for the comma-shred filter bug surfaced during 4.1.13 soak (Dry Eye and other comma-containing values silently zeroed) + tunnel-up.sh credential hardening |
| [4.1.13](prod-rel-4.1.13-handoff.md) | v1.17.30 | P1 Core Focus filter fix (MULTI_CHOICE branch was missing — every selection zeroed dashboards) + first slice of client branding (header brand badge, `/clients/me`, `logoUrl` form input, 4px brand stripe, CSS-var theming) |
| [4.1.12](prod-rel-4.1.12-handoff.md) | v1.17.24 → v1.17.28 | Second wave of 2026-06-02 customer fixes: Demographics Practice Setting multi-select + three missing graphs (educational, social media, valuable content, objectivity) + Sociometric/Total-Weighted column reorder + Educational Resources chart layout + sort-comparator sign fix |
| [4.1.11](prod-rel-4.1.11-handoff.md) | v1.17.17 → v1.17.23 | emailDomains required + auto-approve invitees + TEAM_MEMBER read perms + ZodError→400 + escape hatch removed + lint CI fix + client roles view-only + nomail backfill + Hcp.email required + insights layout density + write-button hide sweep |
| [4.1.10](prod-rel-4.1.10-handoff.md) | v1.17.12 + v1.17.13 + v1.17.14 + v1.17.15 + v1.17.16 | Consolidated 2026-06-02 customer bug bundle: respondent-counting dedup + per-campaign email filter + byCoreFocus MULTI_CHOICE fix (Demographics + KOL Profile) + filter dropdown population + Biased Leaders nomination type + tab rename + 0-result UI + 3 skeleton dimensions + v1.17.16 follow-ups (NUM-extract crash on `".."`, Demographics filter bar mount fix) |
| [4.1.9](prod-rel-4.1.9-handoff.md) | v1.17.10 + v1.17.11 | Insights dashboard perf pass A + B (SQL aggregations, narrower fetches, composite index, threshold cache) |
| [4.1.8](prod-rel-4.1.8-handoff.md) | v1.17.8 + v1.17.9 | Per-client email-domain allowlist + campaign-create excludeInternalEmails fix + deferred E2E deep assertions |
| [4.1.7](prod-rel-4.1.7-handoff.md) | v1.17.7 | Insights full-name search fix + tunable InfluencerThreshold table |
| [4.1.6](prod-rel-4.1.6-handoff.md) | v1.17.6 | Rename-nomination dialog: inline exact-match callout |
| [4.1.5](prod-rel-4.1.5-handoff.md) | v1.17.4 + v1.17.5 | Bundled (state whitelist + respondent filters) |
| [4.1.4](prod-rel-4.1.4-handoff.md) | v1.17.3 | Clear-filters consistency across 5 Insights surfaces + nav grouping |
| [4.1.3](prod-rel-4.1.3-handoff.md) | v1.17.2 | P1 hotfix — admin users blocked + 2 queued UX items |
| [4.1.2](prod-rel-4.1.2-handoff.md) | v1.17.1 | Patch from 4.1.1 soak |
| [4.1.1](prod-rel-4.1.1-handoff.md) | v1.16.x | Phase 3 PR B/C/D — completes the Phase 3 arc |
| [4.0](prod-rel-4.0-handoff.md) | v1.16.0 | Phase 3 PR A — campaign-scoring teardown |
| [3.3](prod-rel-3.3-handoff.md) | v1.15.31 | Two follow-ups on 3.2 |
| [3.2](prod-rel-3.2-handoff.md) | v1.15.x | KOL Analysis cutover |

Fossils (caught in review, not deployed — kept for audit trail): `prod-rel-3.1`, `prod-rel-4.1`. See those tags directly; no handoff doc was cut.

## Tag → commit lookup

```bash
git tag -l 'prod-rel-*' | sort -V    # all release tags, version-sorted
git show prod-rel-4.1.7 --no-patch   # commit + tag message for one
```
